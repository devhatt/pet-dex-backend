name: Go CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.21"

      # - name: Install dependencies
      #   run: go mod tidy

      # - name: Build
      #   run: go build -v ./...

      # - name: Run
      #   run: make run

      - name: Create .env
        run: cp .env.example .env

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Build Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: false
          load: true
          tags: pet-dex-api:latest

      - name: Run Docker image
        run: |
          docker run -d -p 3000:3000 pet-dex-api:latest

      # - name: Compose-Dev
      #   run: make compose-dev

      # # - name: Compose-Prod
      # #   run: make compose-prod

      # - name: Run-Api
      #   run: make run-api
